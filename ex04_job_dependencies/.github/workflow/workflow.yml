name: Steps job dependencies Workflow

on: [push]

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: print testing backend
        run: echo "Backend testt passing"
  
  frontend:
    runs-on: ubuntu-latest
    needs: backend
    steps:
      - name: checkout code
        uses: actions/checkout@v4
        
      - name: print testing frontend
        run: echo "Building frontend after backend success"  